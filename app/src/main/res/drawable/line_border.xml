<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <stroke
        android:color="@color/colorAccent"
        android:dashWidth="10px"
        android:dashGap="10px"
        android:width="1dp"/>

    <gradient
        android:angle="90"
        android:centerColor="@color/colorNavIcon"
        android:endColor="@color/colorNavIcon"
        android:startColor="@color/colorNavIcon" />
</shape>